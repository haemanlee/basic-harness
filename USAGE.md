# techedu 사용 가이드

이 문서는 **처음 이 시스템을 쓰는 팀**을 위한 실무 가이드입니다. 무엇을 프롬프트로 넣고, 어디서 사람이 개입하고, 어떻게 하면 잘 쓰는지를 다룹니다.

> 내부 동작 규칙(에이전트 간 통신, 재시도 카운터 계약 등)은 [`CLAUDE.md`](./CLAUDE.md)에 있습니다. 이 문서는 철저히 "사용자가 무엇을 하면 되는가" 관점입니다.

---

## 1. 한눈에 보기

이 저장소는 **PRD/기획서를 넣으면 검토 → 구현 → 기능 검증 → 보안/통합 검증 → 마무리까지 자동으로 오케스트레이션**하는 파이프라인입니다. 최상위 대화 세션 자체가 "리더"가 되어, 5개 전문 에이전트(팀원)와 여러 스킬을 조율합니다. 별도로, 이미 작성된 PR/diff를 리뷰하는 **PR 팀 리뷰**도 독립적으로 쓸 수 있습니다.

### 언제 무엇을 쓰나

| 상황 | 시작 방법 | 동작 |
|---|---|---|
| 텍스트/마크다운 PRD로 구현까지 | PRD 경로/내용을 주며 "이 PRD로 진행해줘" | 파이프라인 1→4단계 자동 |
| PRD가 PowerPoint(`.pptx`) | "기획서 PPT …로 진행해줘" | `prd-delta-extractor`가 먼저 구조화 → 파이프라인 |
| PRD 완전성만 먼저 점검 | "이 PRD 완전성 검토만 해줘" | `prd-review`만 실행 |
| 이미 있는 PR/브랜치/diff 리뷰 | "PR #42 리뷰해줘" | `pr-team-review`(파이프라인과 독립) |

전체 흐름 다이어그램은 [`CLAUDE.md`의 파이프라인 다이어그램](./CLAUDE.md#파이프라인-다이어그램)을 참고하세요.

### 구성 요소

- **팀원 에이전트** (`.claude/agents/`): `backend-implementer`(구현+단위테스트), `code-verifier`(기능 검증), `security-reviewer`(보안), `integration-tester`(통합/E2E), `prd-completion-assistant`(PRD 보완 초안).
- **스킬** (`.claude/skills/`): `prd-review`, `prd-delta-extractor`, `convention-check`, `pr-description-generator`, `retry-postmortem`, `pr-team-review`.

---

## 2. 팀 도입·운영 (설치 / 권한 / CI)

### 2.1 파일 배치

이 시스템은 대상 프로젝트의 `.claude/` 디렉토리와 최상위 `CLAUDE.md`로 동작합니다. 도입하는 방법 두 가지:

- **프로젝트에 직접 얹기**: 실제 백엔드 저장소 루트에 `CLAUDE.md`, `.claude/agents/`, `.claude/skills/`를 복사합니다. 그러면 그 저장소에서 Claude Code를 열 때 파이프라인이 활성화됩니다.
- **조직 공용 템플릿으로 두기**: 이 저장소(`basic-harness`)를 템플릿으로 삼아 새 프로젝트가 복사해가도록 합니다.

핵심은 **구현 대상 코드가 있는 저장소에서 세션을 여는 것**입니다. 그래야 `backend-implementer`가 기존 코드 컨벤션을 읽고 따르며, 검증 팀원이 실제 빌드/테스트를 돌릴 수 있습니다.

### 2.2 권한 (settings)

검증 팀원(`code-verifier`, `integration-tester` 등)은 빌드·테스트를 실행하므로, 해당 명령을 매번 승인창 없이 돌리려면 팀 공용 `.claude/settings.json`에 allow 규칙을 둡니다:

```jsonc
{
  "permissions": {
    "allow": [
      "Bash(./gradlew *)",
      "Bash(mvn *)",
      "Bash(git status *)",
      "Bash(git diff *)"
    ]
  }
}
```

- **팀 공용 규칙은 `settings.json`** 에 둡니다(저장소에 커밋되어 팀원 모두에게 적용).
- **`settings.local.json`은 개인용**이며 `.gitignore`에 있어 커밋되지 않습니다 — 개인 로컬 편의 규칙만 여기 둡니다.

### 2.3 의존성

- `prd-delta-extractor`(pptx PRD 처리)를 쓰려면 로컬에 `libreoffice`(또는 `soffice`), `pdftoppm`(poppler-utils), Python `Pillow`, `PyYAML`이 필요합니다. 없으면 스킬이 조용히 우회하지 않고 **"설치해달라"고 사용자에게 알립니다**(전체 덱을 vision으로 처리해 토큰을 낭비하지 않기 위한 설계).
- 나머지 스킬과 파이프라인은 별도 시스템 의존성 없이 동작합니다.

### 2.4 CI로 쓸 때의 경계

이 파이프라인은 **검증 판정과 텍스트 산출물(PR 초안 등)을 만들 뿐, 실제 git 커밋/PR 생성/머지를 자동으로 하지 않습니다**:

- `pr-description-generator`는 PR 제목/본문 **초안 텍스트만** 만들고 실제 PR을 열지 않습니다.
- `convention-check`는 **참고용(비차단)** 이라 파이프라인을 막지 않습니다.

따라서 "머지 여부"는 언제나 사람이 판정 결과를 받아 결정합니다. CI 게이트로 활용하려면, 파이프라인 판정(PASS/FAIL/FIX/REDO)을 사람이 확인하는 리뷰 단계로 두고, 실제 머지는 팀의 기존 승인 절차를 따르세요.

---

## 3. 시나리오 A — PRD 파이프라인 실무 사용법

### 3.1 시작하기 (예시 프롬프트)

**(a) PRD 하나로 구현까지 전부 자동으로**
```
docs/order-api-prd.md 이 PRD로 파이프라인 돌려서 구현까지 진행해줘.
스택은 Spring Boot 3.x / Java 17이고 기존 레이어 컨벤션(Controller-Service-Repository) 따라줘.
```

**(b) 완전성 검토만 먼저 (구현 전에 PRD 품질 확인)**
```
이 PRD 완전성 검토만 먼저 해줘. (내용 붙여넣기 또는 경로)
```
→ `prd-review`만 돌아 심각도별(critical/major/minor) 이슈 목록을 줍니다. critical이 없으면 그때 "이어서 구현까지 진행해줘"라고 하면 됩니다.

**(c) 기획서가 PowerPoint일 때**
```
기획서 PPT(specs/checkout-v2.pptx) 지난 버전이랑 뭐가 바뀌었는지 뽑고 리뷰해줘.
```
→ `prd-delta-extractor`가 변경된 슬라이드만 추출해 `spec.yaml`/`delta.md`를 만든 뒤, 그 결과로 `prd-review`가 이어집니다.

### 3.2 단계별로 무엇을 보게 되고, 어디서 개입하나

| 단계 | 담당 | 사용자가 받는 것 | 개입 지점 |
|---|---|---|---|
| 1. 완전성 검토 | `prd-review`(리더) | 심각도별 이슈 목록 | **critical이 있으면 여기서 멈춤** |
| 1a. 보완 초안 | `prd-completion-assistant` | 누락 항목별 제안 텍스트 | 초안을 **직접 PRD에 반영**한 뒤 "다시 검토해줘" |
| 2. 구현 | `backend-implementer` | 구현 파일 + 단위테스트 요약 | — |
| 3a. 기능 검증 | `code-verifier` | PASS/FAIL 판정 | FAIL이면 자동 재작업(최대 3회) |
| 3b. 보안/통합 | `security-reviewer` + `integration-tester` | 각 PASS/FIX/REDO | FIX/REDO면 자동 재작업(최대 2회) |
| 4. 마무리 | 리더 | 컨벤션 참고 + PR 초안 | PR 실제 생성은 **직접** |

- **critical 이슈**: 파이프라인이 멈추고 보완 초안을 제안합니다. 초안은 **자동으로 PRD에 반영되지 않습니다** — 사람이 검토·반영한 뒤 다시 제공해야 1단계가 재실행됩니다.
- **재시도 한도 초과**(3a 3회 / 3b 2회): 자동 재시도를 멈추고 `retry-postmortem`이 시도 이력·반복 패턴·핵심 쟁점을 요약해 줍니다. 그다음 진행은 **사람이 판단**합니다.

### 3.3 권장 운영 방식

- **한 대화 = 하나의 PRD**를 끝까지. "리더는 하나(이 세션)"라는 전제 때문에, 한 세션에서 여러 PRD를 뒤섞으면 맥락이 흐려집니다. 새 PRD는 새 대화에서 시작하세요.
- 재작업이 필요하면 **새 대화를 열지 말고 같은 세션에서 이어서** 요청하세요. 그래야 팀원이 이전 작업 맥락(무엇을 왜 그렇게 했는지)을 유지한 채 국소 수정을 합니다.

---

## 4. 시나리오 B — PR 팀 리뷰 사용법

이미 작성된 PR/브랜치/diff를 리뷰합니다. PRD 파이프라인과 **독립적**으로 동작합니다.

### 4.1 시작하기 (예시 프롬프트)

```
PR #42 리뷰해줘.
```
```
feature/checkout-refactor 브랜치 코드 리뷰 팀 붙여줘.
```
```
(diff 붙여넣기) 이 변경 리뷰해줘.
```

### 4.2 흐름

1. **Phase 1 — 기능 게이트** (`code-verifier` 단독): PR 설명/연결된 이슈/커밋 메시지 + 기존·신규 테스트를 기준으로 기능적 정확성을 봅니다. PRD가 없으므로 이 기준으로 대체 판단합니다.
   - **FAIL이면 Phase 2로 넘어가지 않고 종료**됩니다(틀린 코드에 비싼 검증을 낭비하지 않기 위함).
2. **Phase 2 — 피어 팀** (`pr-security-reviewer` + `pr-integration-tester`, 병렬): Phase 1을 통과한 코드에 대해서만 보안·통합을 병렬로 검토합니다. 두 검토자는 교차 발견 사항을 서로 직접 공유하며(피어 통신), 판정은 리더가 통합합니다.
   - 이 두 피어는 PRD 파이프라인의 `security-reviewer`/`integration-tester`와는 **별개의 전용 에이전트**입니다. 파이프라인 리뷰어는 리더-중계만 하도록 `SendMessage`를 아예 갖지 않고, PR 리뷰 피어만 직접 통신 권한을 갖습니다 — 일반 PRD 실행이 이 권한의 영향을 받지 않도록 구조적으로 분리한 것입니다.
   - **리뷰 전용 보장**: `pr-integration-tester`가 PR을 기동·검증하려고 통합 테스트를 쓸 수는 있지만, 그것은 일회용 스캐폴딩이라 **보고 전에 모두 제거**되어 대상 PR 브랜치/diff가 오염되지 않습니다(가능하면 일회용 워크트리에서 격리 실행). 리뷰가 끝나면 워킹 트리가 시작 시점과 동일함을 확인해 보고합니다.
3. **종합 판정**: 승인 가능 / 국소 수정 필요 / 재설계 필요 중 하나로 정리되고, 팀 해체(사용한 리뷰어 정리 + 워킹 트리 정결 확인) 보고까지 받습니다.

### 4.3 주의

- 같은 세션에서 **다른 PR**을 이어서 리뷰하려면, 이전 리뷰 팀을 정리한 뒤 새로 시작합니다("PR #43도 리뷰해줘"라고 하면 리더가 이전 팀을 해체하고 새 팀을 꾸립니다). 같은 PR의 재검토(수정 후 다시 봐줘)는 기존 팀을 이어서 씁니다.
- 이 스킬은 기본적으로 **리뷰만** 하고 코드를 고치지 않습니다. 고치려면 "발견된 문제 고쳐줘"처럼 명시적으로 요청하세요(그때 별도로 구현 에이전트 사용 여부를 확인합니다).

---

## 5. 프롬프트 작성 원칙 (Do)

**좋은 PRD가 재작업 루프를 줄입니다.** `prd-review` 체크리스트가 곧 "좋은 PRD의 목록"이니, 넣기 전에 아래를 담으세요:

- **성공 기준(DoD)**: "무엇이 되면 완료인가"를 검증 가능한 형태로. ("빠름" ❌ → "p95 200ms 이하 @100rps" ✅)
- **예외·엣지 케이스**: 실패/오류 흐름, 경계값, 빈 값/최대값, 동시성 처리 방침.
- **비기능 요구**: 인증/인가, 성능, 로깅, 보안 요건.
- **데이터 정의**: 입출력 구조, 필수/선택 필드.

**기술 맥락을 명시하세요.**
- 스택·버전(예: Spring Boot 3.x, Java 17), 기존 레이어/네이밍 컨벤션을 알려주면 `backend-implementer`가 새 패턴을 지어내지 않고 기존 것을 따릅니다.

**진행 방식을 분명히.**
- 특정 단계만 원하면 명시("검토만", "구현까지", "3b 보안만 다시").
- 재작업·후속 요청은 **같은 세션에서 이어서** — 맥락 유지가 품질을 좌우합니다.

---

## 6. 안티패턴 (Don't)

- **모호한 PRD로 곧장 구현 시키기.** critical 게이트에서 막히거나(다행), 더 나쁘게는 잘못된 방향으로 구현됩니다. 먼저 완전성 검토로 걸러내세요.
- **검증 팀원에게 코드를 고치라고 시키기.** `code-verifier`/`security-reviewer`/`integration-tester`는 **판정만** 합니다(책임 분리). 프로덕션 코드 수정은 `backend-implementer`의 몫입니다. "code-verifier한테 고치라고 해줘"는 설계상 하지 않습니다.
- **한 세션에 무관한 PRD/PR을 뒤섞기.** 맥락이 오염됩니다. PRD는 대화 단위로, PR 리뷰는 대상별로 팀을 해체·재시작하세요.
- **에스컬레이션을 무시하고 "다시 해봐"만 반복.** 재시도 한도(3a 3회 / 3b 2회)의 목적은 무한 루프를 막고 **사람 판단을 부르는 것**입니다. 한도에 닿으면 `retry-postmortem` 요약을 읽고, 반복 실패의 근본 원인(대개 PRD 모호함)을 먼저 해결하세요.
- **텍스트 산출물을 실제 실행으로 착각하기.** PR 초안은 초안일 뿐 PR을 열지 않고, 컨벤션 점검은 비차단 참고입니다. 실제 git/PR/머지는 사람이 합니다.

---

## 7. 트러블슈팅 / 운영 팁

- **재시도 이력이 궁금하면**: 시도 회차와 판정은 공유 작업 목록의 task metadata에 구조적으로 남습니다. 에스컬레이션 시 `retry-postmortem` 요약이 이 metadata를 1차 근거로 정리해 줍니다. 대화가 길어져 요약·압축돼도 이력은 태스크에 남습니다.
- **"예상치 못한 변경" 경고가 뜨면**: 검증 팀원은 작업 종료 직전 `git status --short`로 자신이 파일을 건드리지 않았는지 스스로 확인해 보고합니다. 여기에 예상치 못한 변경이 보고되면 **다음 단계로 진행하지 말고** 무엇이 바뀌었는지 먼저 확인하세요.
- **단계가 건너뛰어진 것 같으면**: 설계상 단계는 건너뛰지 않습니다 — critical PRD로 구현을 시작하지 않고, 3a(기능)를 통과 못 한 코드로 3b(보안/통합)를 돌리지 않습니다. 건너뛴 듯 보이면 앞 단계가 조기 종료(예: critical 이슈로 멈춤)된 것은 아닌지 확인하세요.
